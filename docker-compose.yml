version: '3.8'
services:
  livekit-agent:
    build: .
    env_file:
      - .env
    volumes:
      - .:/app # Mapeia o diretório atual para o diretório /app no container (útil para desenvolvimento)
